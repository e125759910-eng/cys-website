# 手动执行 Vercel 部署命令

由于 Vercel CLI 需要交互式输入（浏览器登录、项目配置等），您需要在命令行中手动执行以下步骤：

## 执行步骤

### 1. 打开命令提示符或 PowerShell

在项目目录下打开终端：
```bash
cd C:\Users\User\cys-website
```

### 2. 登录 Vercel

```bash
vercel login
```

这会：
- 打开浏览器让您登录 Vercel
- 如果没有账号，可以先注册
- 登录成功后，命令行会显示确认信息

### 3. 首次部署（配置项目）

```bash
vercel
```

这会询问您：
- **Set up and deploy?** → 输入 `Y` (Yes)
- **Which scope?** → 选择您的账号
- **Link to existing project?** → 如果是新项目，输入 `N` (No)
- **What's your project's name?** → 输入项目名称，例如：`cys-website`
- **In which directory is your code located?** → 直接按回车（使用 `./`）
- **Want to override the settings?** → 输入 `N` (No)

### 4. 部署到生产环境

```bash
vercel --prod
```

这会部署到生产环境并获得一个生产 URL。

## 或者使用批处理脚本

我已经创建了一个批处理脚本 `部署到Vercel.bat`，您可以双击运行它，它会自动执行所有步骤。

## 部署后的重要配置

### 1. 创建 KV 数据库

1. 访问 https://vercel.com/dashboard
2. 选择您的项目
3. 点击 "Storage" 标签
4. 点击 "Create Database"
5. 选择 "KV"
6. 命名为 `cys-warranties` 并创建
7. KV 的环境变量会自动添加

### 2. 设置环境变量

1. 在项目 Dashboard 中，点击 "Settings"
2. 点击 "Environment Variables"
3. 添加以下变量：

   **变量 1:**
   - Name: `ADMIN_USERNAME`
   - Value: `admin`
   - Environment: 选择所有环境（Production, Preview, Development）

   **变量 2:**
   - Name: `ADMIN_PASSWORD`
   - Value: `您的安全密码`
   - Environment: 选择所有环境

4. 点击 "Save"

### 3. 重新部署以应用环境变量

1. 在 "Deployments" 标签中
2. 找到最新的部署
3. 点击右侧的 "..." 菜单
4. 选择 "Redeploy"
5. 确认重新部署

## 验证部署

部署完成后：
1. 访问您的网站 URL（Vercel 会显示）
2. 访问 `/admin/login` 测试后台登录
3. 新增保固资料测试功能

## 故障排除

如果遇到问题：
- 检查 Vercel CLI 是否正确安装：`vercel --version`
- 检查是否已登录：`vercel whoami`
- 查看部署日志：在 Vercel Dashboard 中查看

---

**提示**：首次部署后，每次推送代码到 Git 仓库会自动触发新的部署！


