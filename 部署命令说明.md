# Vercel 部署命令说明

## ⚠️ 首次部署需要交互式配置

由于首次部署到 Vercel 需要：
1. 登录 Vercel 账号
2. 选择/创建项目
3. 配置项目设置

这些步骤需要交互式输入，无法完全自动化。

## 🚀 推荐方式：通过 Vercel Dashboard

### 步骤：

1. **访问 Vercel Dashboard**
   - 打开 https://vercel.com/new
   - 如果没有账号，点击 "Sign Up" 注册

2. **导入项目**
   - 点击 "Import Git Repository"
   - 选择您的 Git 仓库（GitHub/GitLab/Bitbucket）
   - 如果还没有连接，先连接您的 Git 账号

3. **配置项目**
   - Framework Preset: Next.js（会自动检测）
   - Root Directory: `./`
   - 其他设置保持默认

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待构建完成（1-3分钟）

5. **创建 KV 数据库**
   - 部署完成后，进入项目 Dashboard
   - 点击 "Storage" → "Create Database"
   - 选择 "KV"
   - 命名并创建（例如：`cys-warranties`）
   - KV 的环境变量会自动添加

6. **设置环境变量**
   - 进入项目设置 → "Environment Variables"
   - 添加：
     ```
     ADMIN_USERNAME=admin
     ADMIN_PASSWORD=您的密码
     ```

7. **重新部署**
   - 点击 "Deployments" 标签
   - 找到最新的部署，点击 "..." → "Redeploy"
   - 或直接推送新的 commit 触发自动部署

## 📝 通过 CLI 部署（需要先登录）

如果您想使用命令行，需要先完成交互式登录：

```bash
# 1. 登录 Vercel（需要打开浏览器）
vercel login

# 2. 首次部署（交互式配置）
vercel

# 3. 后续重新部署到生产环境
vercel --prod
```

## ✅ 部署后的验证

部署完成后：
1. 访问您的网站 URL（Vercel 会提供）
2. 访问 `/admin/login` 测试后台登录
3. 新增保固资料测试功能

---

**建议：** 首次部署使用 Dashboard 更方便，后续更新可以自动触发（推送代码到 Git）。


